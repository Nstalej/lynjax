# Lynjax backend - runtime dependencies
# Pinned to minor ranges: reproducible enough to trust, loose enough to patch.

# API
fastapi~=0.115.0
uvicorn[standard]~=0.34.0

# Configuration
pydantic~=2.10.0
pydantic-settings~=2.7.0
platformdirs~=4.3.0

# Database
aiosqlite~=0.20.0

# Credential vault (Fernet)
cryptography~=44.0.0

# HTTP client (REST connector, also used by the test client)
httpx~=0.28.0

# SSH connector
paramiko~=3.5.0

# SNMP connector
pysnmp~=7.1.0

# REST connector (Sophos XML profile)
lxml~=5.3.0
